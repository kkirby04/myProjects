if __name__ == '__main__':
    # Read an integer from STDIN
    n = int(input())
    
    # Iterate through the range of non-negative integers less than n
    for i in range(n):
        # Calculate the square of each number
        square = i**2
        
        # Print the square on a separate line
        print(square)
